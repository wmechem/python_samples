Very simple Flask app that uploads a file, parses it, and validates the contents against various rules, then stores it in a database.

See acme_readme.txt and instructions.txt for more info, setup etc.
